Wrote docs/commands-reference.md: every system command in the manual's own groups, with what it does, what it replies and what it refuses, every reply run through the binary before being written down. Added the two sections a reader could not get anywhere else -- what sw-apl does not have, per command rather than by inference ()NUMBER, )OFF HOLD, )CONTINUE HOLD, )MSG, )MSGN, )OPR, )OPRN, )PORTS and the LOCK/KEY passwords, all the multi-user surface of a shared machine), and what sw-apl has that APL/360 did not (--library, --ws-size). )MSGN and )OPRN were not in parity.md's row at all. Writing it found three behaviours that are wrong rather than undocumented, none fixed here: )COPY and )PCOPY print nothing where the manual gives both a SAVED line and )PCOPY a NOT COPIED list, which is now step 004 and a parity row; )WIDTH accepts 30 to 254 where the manual says 30 to 130, deliberate but never recorded as a deviation until now. Linked from README and session.md, which keeps its tables. No code change. All gates green, 76 reg-rs tests, sw-checklist 194 passed 0 failed.