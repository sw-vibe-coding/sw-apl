Owner request: generate the glyph tables from one serialized source instead of duplicating them. Create data/glyphs.toml as the single source of truth (every APL\360 glyph with code point, name, monadic and dyadic meaning; the lookalike table; the later-APL glyph table). Add a build.rs to apl-value (and apl-lex if it needs the primitive set) that reads it and emits Rust consts into OUT_DIR, included with include!(concat!(env!("OUT_DIR"), "/glyphs.rs")). Regenerate docs/glyphs.txt from the same file with a script so the docs cannot drift. Keep a test that pins a few entries end to end. TDD; parity; update docs/architecture.md with the codegen phase.