Phase 2 step 1 (docs/plan.md). In apl-prims-mixed (split a new crate if the module gate needs it): catenate along the last axis for rank >= 2 (LENGTH ERROR when the other axes differ), first-axis form (⊖ is not it: use the axis bracket A,[1]B and the parser's Function::Prim(',') with axis), laminate when the axis is fractional (new axis of length 2), scalar extension of a scalar argument to the needed shape, ravel unchanged. The eval apply module must pass the axis value through instead of NOT IMPLEMENTED for ','. TDD; update docs/parity.md; seed reg-rs for samples 08 and 44 if they run fully.