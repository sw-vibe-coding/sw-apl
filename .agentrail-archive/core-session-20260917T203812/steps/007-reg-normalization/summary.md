Adopted reg-rs preprocess filtering, after the owner pointed out the framework is built for output that is not byte-identical twice and sw-apl was using none of it. reg-rs create takes -P/--preprocess, -M/--diff-mode, --expects and --flaky-note; every .rgt here carried only command, timeout, exit_code and desc, and docs/testing.md never mentioned the options existed. Read sw-ml-study/sw-mlpl first, where every demo baseline carries a shared sed filter masking an SVG byte count and float tails. Added scripts/normalize-apl-output.sh, which masks what follows an upper-case label ending in a VARIES marker, and only on an output line, since output starts in column one while an echoed input line is indented six spaces or headed by its bracketed prompt -- so the statement stays legible beside its masked answer. APL prints bare numbers, so a filter cannot tell a clock reading from arithmetic and the sample has to say which is which; the label is ordinary mixed output that reads as part of the session. scripts/reg-seed.sh now gives the filter to every test and gained an --all mode to recreate the corpus. Rewrote samples/58-ibeams.apl to print the real values of I-beams 20, 21, 24 and 25 instead of hiding them behind predicates that merely proved them sensible; the sample now documents what an I-beam returns. Proved rather than assumed: copied the .out files before the recreate and diffed after, showing every .rgt gained exactly one line and no transcript changed but 58's. The --all recreate then swept back three samples that step 002 had deliberately left unseeded, which is how they arrived once before, so the reason each is unseeded now lives in the script rather than in my head: 11-system-cmds would pin two system commands as INCORRECT COMMAND until Phase 4, and 14-multiline is a comment-only leftover from sw-cor24-apl describing commands that are not APL\360. The third, 24-lazy-iota, is correct and deterministic and had no reason beyond never having been planned, so it is seeded; 46 transcripts now. docs/testing.md gained the section whose absence let this go unnoticed, including when to reach for --flaky-note instead of widening the filter, and the rule that masking is for what cannot reproduce and never for a value that is merely inconvenient. All gates clean, 264 tests. Commit fb1c1a6.