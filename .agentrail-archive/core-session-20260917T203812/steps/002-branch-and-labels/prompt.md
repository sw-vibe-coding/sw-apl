Phase 3 step 2. Labels (NAME: at the start of a body line) are local constants holding the line number. → with an expression: the first element of the value selects the next line; an empty vector falls through to the next line; 0 or a line number outside the function exits (returning R when the header has one). Implement the line counter so →LABEL×⍳COND works. After this, samples/50-horse-race.apl must run: verify it, seed its baseline, and add a function form of Conway's Life as samples/54-life-function.apl with its baseline. TDD; parity rows.