Phase 3 step 4. Errors inside a defined function print FN[n] before the error name, echo the failing statement and caret, and suspend the function. )SI lists the suspended functions innermost first, )SIV adds their local names. A bare → clears the top entry; → with a line number resumes there. Nested suspensions stack. DEPTH ERROR when the state indicator grows past a sane limit. TDD; parity rows; sample.