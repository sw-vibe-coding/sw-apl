An executable .apl file. sw-apl drops a first line beginning with a hash and a bang, since the kernel has already acted on it and the line is the shell's rather than APL's; before, it was read as APL and answered CHARACTER ERROR with an echo and a caret, so a script worked but its transcript opened with three lines of noise. Only the first line and only those two characters, because the hash is not an APL\360 character and stays a CHARACTER ERROR everywhere else, which one of the new tests pins; the rule lives in the batch line splitter beside the one that tolerates CRLF, so the interactive session is untouched. Documented in docs/session.md and the help MODES block rather than left to be discovered: the flags must come before -f or -f takes the flag as its filename, and the env -S form is the portable one because a bare shebang splits its arguments on macOS but not on Linux. The bare form is still tested, since it is what someone will write first. The tests are reg-rs tests rather than additions to cli_tests.rs, because they check how the binary answers the shell, which is reg-rs's job, and step 10 moves the rest of that file across: tests/scripts holds three executable .apl files and the new scripts/reg-seed-cli.sh seeds them, beside a fourth that runs the same file through -f to show the shebang is skipped either way. 51 transcripts. docs/session.md also gained the section the step asked for, covering the four ways to run a file, the here-document form for putting a program inside a shell script, and the executable form. All gates clean. Commit 55498f6.