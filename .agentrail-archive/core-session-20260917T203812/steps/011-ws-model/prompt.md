Phase 4 step 1 (docs/plan.md, saga workspaces). This step opens a new saga; run agentrail next first and follow what it says about the saga rather than assuming this is still core-session. The workspace as a value: the symbol table of variables and functions, the settings saved with it (index origin, print precision, print width, the random link), the state indicator, and the workspace identifier that )WSID reports. Today Workspace holds all of that plus a console and a clock, which are the terminal's and must not be saved; separate what belongs to the workspace from what belongs to the session it is running in, so a later step can write one out and read it back. )CLEAR gives a fresh workspace and prints CLEAR WS; )WSID shows or sets the identifier. Decide what a cleared workspace does to a suspended function and say so in docs/session.md. TDD; update docs/parity.md rows in the same commit; add a sample and seed it; commit, push, report.