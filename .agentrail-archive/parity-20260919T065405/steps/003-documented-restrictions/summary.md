Gave docs/parity.md a Restrictions section and moved five rows into it: a saved workspace does not keep a suspended function (design.md D7, the price of a re-executable text file), a call inside a larger expression does not suspend (the evaluator is a recursive walk), a locked function is obscured rather than hidden (D14), )SYMBOLS reports but cannot be set, and WS LOCKED and NOT SAVED WS QUOTA USED UP cannot arise. Each says the decision it follows from, so nobody sets about implementing one. Sorting the rows turned up two things: one row was a real gap rather than a restriction and is now step 004 -- an open quote-quad output line survives inside a function but not across a statement in immediate execution, because Reply carries complete lines -- and one row had quietly become done, a stale 'no literals yet' qualifier from before the lexer had string literals, whose sample reference was wrong as well. The file now opens by saying how many rows are open and what they are, so the summary is checkable: 111 done, 1 todo, 5 restrictions, nothing unaccounted for. Also corrected the out-of-scope note, which called the multi-user commands 'honest stub replies' when they answer INCORRECT COMMAND. No code change.