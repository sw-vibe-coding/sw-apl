Glyph table marks execute and format as mode B (MODE_B); tokenize and parse take the mode's glyphs, so A refuses them with the same CHARACTER ERROR. New components/b75 with apl-execute: statement-level execute shows what the line shows, expression-level must give a value (VALUE ERROR otherwise), errors caret on the execute. Format is NONCE in B until step 018. Sample harness runs samples whose first line is MODES (B) with mode 75; sample 76. reg-rs 85 of 85, check-pages passed, pages rebuilt. Overstrike pairs for execute and format carried to step 016. Pushed.