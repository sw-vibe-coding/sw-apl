Mode on the workspace and libraries by mode. New crates apl-modes (Mode, Modes, MODES line, Host), apl-shelves (mode-aware list, read, keep, forget; two workspaces may share a name as NAME and NAME@B), apl-uses (modes from code: I-beam or group is A only; execute, format, quad names are B only). Writer puts MODES second and the settings as ORIGIN, DIGITS, WIDTH directives; directives apply wherever the line comes from. Fixed: LINK was written but never read. --mode 68 or 75 on sw-apl and sw-apl-server; wasm reads a mode field. lib1 workspaces regenerated as (A)(B); pages rebuilt. reg-rs 82 of 82 with two rebases for the format change and the new flag. Browser check skipped: playwright-core not installed and Chrome extension not connected. Pushed to main.