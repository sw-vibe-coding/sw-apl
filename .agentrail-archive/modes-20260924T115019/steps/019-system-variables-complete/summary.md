System variables completed in B. The comparison tolerance can be set and is threaded through the primitives and saved with a CT directive. A setting (CT IO PP PW RL) can be made local in a function header, keeps its value until assigned, and is given back on return. Indexed assignment into a system variable works. PW 128 during a definition is resolved as not needed, since the editor never wraps. Found: quad output inside a function renders with the settings at the end of the statement; added as a step after TTTML. reg-rs 88/88, check-pages passed.