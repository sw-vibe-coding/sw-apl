Owner discussion 2026-09-22 (TTTML): output reaches the terminal only at a read or at the end of a statement, so TRAIN 6000's progress lines, printed every 1000 games, all appear together when it finishes -- in the CLI and in the browser, where a long statement looks frozen. A 2741 printed as the carriage moved, and a 5110 as the screen scrolled.

Make output appear as it is written. Since step 022 a function's output is rendered when written (Output::Lines); send it on then. The Console trait gains a way to show lines without reading; the CLI prints them at once; the browser worker sends a frame of lines that does not ask for input and the page prints them and keeps waiting; the service's WebSocket and aplterm's link carry the same. Batch transcripts must not change: reg-rs proves (A) and (B) unmoved. check-pages: TRAIN in TTTML shows its first progress line before the statement ends.
