Owner direction 2026-09-23: the README is out of date, its status section stale, and it needs organizing so it is easier to read; it must say how to use aplterm with each mode. Rewrite it around what a reader wants: what sw-apl is and its two modes, trying it in the browser, running it locally, the 2741 terminal and the service in each mode (and both at once), library 1, what works, and the documents. ASCII only (sw-markdown-checker); what and how, no plans or dates. just demo takes a mode.
