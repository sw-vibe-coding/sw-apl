Overstrikes are mode aware: OVERSTRIKE_B holds the 5100 pairs for execute and format; apl-strike takes the mode's extra pairs; Mode::glyphs, overstrikes and letter are the one mapping; CLI composes for --mode; frames carry the mode letter and aplterm follows it; the browser Board is built for a mode (A until step 018). A still refuses the pairs with the same words. Tests per crate, sample 76 extended and rebased, reg-rs 85 of 85, check-pages passed. Pushed.