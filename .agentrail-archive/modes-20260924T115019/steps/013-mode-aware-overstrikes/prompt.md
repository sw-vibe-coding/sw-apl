Owner, 2026-09-21: the overstruck character table and logic must be mode aware. The IBM 5100 APL manual forms execute by striking ⊥ and ∘ and format by ⊤ and ∘; in (B) those pairs form ⍎ and ⍕, and in (A) they stay an illegitimate overstrike exactly as now (the same CHARACTER ERROR words). Data: the two pairs in data/glyphs.toml's [[overstrike]] with mode = "B", generated as a separate OVERSTRIKE_B table, the (A) table unchanged. Logic: apl-strike's strike, compose, read and the Strike machine take the mode's extra pairs, as the lexer takes the mode's extra glyphs. One place maps a mode to its extras (apl-modes), used by eval_line and every front end. Front ends: the CLI composes in-process with the host's mode; aplterm composes client-side, so the server's frame carries the mode and aplterm's keyboard follows it; the browser board gets a mode setter for step 017's tab (the page sends none yet, so it composes as (A)). The keyboard picture stays step 016's. TDD: strike tests per mode, a CLI transcript overstriking ⊥∘ in (B) and in (A), a keyboard test, a frame test. reg-rs for (A) unchanged. Gates, pages, check-pages, commit, push.