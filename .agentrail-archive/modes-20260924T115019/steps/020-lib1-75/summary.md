Library 1 per mode. Files are named for their modes: NAME, NAME.a-70, NAME.b-75; the shelves key saves that way, rename a workspace narrowed to one mode, and still read old at-sign keys. check-provenance checks names against modes lines. BIRDS.b-75 uses execute for every bird including M, Y and KI; BIRDS.a-70 is the old one. The wasm bundle ships both. Sample 80 is new. Milestone 2 reached. reg-rs 89/89, check-pages passed.