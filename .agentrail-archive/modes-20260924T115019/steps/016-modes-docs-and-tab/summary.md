The (B) '75 tab in the browser (Board(MODE), a mode field for the worker, a dialog warning that the workspace in hand is lost before a switch, the mode remembered) plus docs for two modes: language.md split into a shared core, what B adds and what A alone has; parity.md with a column per mode; session.md and commands-reference.md list which commands each mode has; README says there are two modes and links mode-b.md. check-pages all passed, reg-rs 87/87, all 65 A samples match with --mode 70. Milestone 2 is claimed at library 1 (step 019), as plan.md says.