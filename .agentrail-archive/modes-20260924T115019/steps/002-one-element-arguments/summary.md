Deal and rotate take a one-element vector as a scalar, so the shuffle idiom A[(⍴A)?⍴A] works. Fixed a test that asserted the bug. Surveyed the other primitives: most already accept one-element arguments, transpose's error is correct, and single-element extension in scalar functions and compress needs the manual, so it is step 005. Sample 41 gains the shuffle at its end, rebased with only additions.