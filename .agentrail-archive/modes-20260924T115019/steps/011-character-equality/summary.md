Equal and not-equal compare characters (APL 360 manual p. 3.8), element by element and in reduction, inner and outer products, via a new Element type in apl-prims-scalar; the numeric fast path is unchanged; a character never equals a number (a stated choice); other functions refuse characters; scan of characters stays DOMAIN ERROR because the result would mix types. Sample 75, unit tests, reg-rs 84 of 84, check-pages passed, pages rebuilt. Pushed.