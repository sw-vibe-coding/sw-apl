Milestone 1. Three commits: in-place split so each 68-only part is a file of its own (apl-settings shared setter created); git mv into components/a68 (apl-ibeam, apl-a68-commands) with the shared clock and hms into apl-clock, wiring only; then the gate: I-beam is NONCE ERROR outside A, ORIGIN DIGITS WIDTH GROUP GRP GRPS are INCORRECT COMMAND outside A, directives work in both. OFF stays shared as the only sign-off. Verified: all unit tests, reg-rs 82 of 82 plain and with explicit mode 68, check-pages all passed in headless Chrome. Pushed to main.