(B) 75 gains the quad system functions: apl-sysfns (which quad names are functions, and applying one: NL, NC, EX), apl-fix (CR and FX), apl-console-control (CC). A quad name that takes an argument parses as a function -- the parser gains Function::System and the evaluator asks apl-sysfns -- so a system variable is still a name and (A) is untouched. CR gives the function flush left without line numbers, and 0 by 0 for anything else, a locked function included: its characters are not to be had by any route. FX takes only what the del editor could have made (header parses, no line the editor reads as a command or a closing del, every line lexes in the mode, the name free, unlocked, not running and not local) and otherwise changes nothing and gives the line at fault, the header being 0, as the 5110 manual reads. EX erases the active referent and leaves a label, a group and a running function. NC and NL classify and list; NL alphabetical. CC checks its arguments and answers 1 or 0 with no device to act on. DL settled as the 5110 variable holding 0, not APLSV's delay. Sample 78; reg-rs 87 of 87, no (A) transcript moved; all gates green. Inserted step 016: a local name does not hide a global function of the same name in either mode, which is an (A) correctness fault and what local function names for FX need. Commit 432333e pushed.