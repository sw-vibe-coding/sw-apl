MVP step 2 of 2 (docs/plan.md Phase 1). Add iota (monadic, quad-IO aware), rho (shape and reshape), ravel and catenate (last axis), the reduce operator on the last axis for the scalar dyadic functions (with identity elements for empty vectors: + 0, × 1, ⌈ smallest, ⌊ largest), quad-IO as an assignable system variable (1 default), and matrix display with right-aligned columns per docs/language.md and docs/session.md. Introduce components/prims (apl-prims-mixed, apl-prims-ops) rather than growing eval. TDD first; then scripts/reg-seed.sh 04, 05, 06 and commit baselines; run scripts/reg.sh run -q to confirm 01-03 still pass. After this step the owner should be able to type +/⍳10 and 2 3⍴⍳6 at the prompt: say so in the report and give a short transcript.