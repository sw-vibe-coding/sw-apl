A workspace holding a locked function is no longer written as plain text. )SAVE writes a three-line preamble and the rest rot-13; )LOAD and )COPY read both forms, telling them apart by the first line. A workspace with nothing locked stays plain, because readable and re-executable is the format's whole virtue. The code and docs say plainly that this is obscuring and not encryption: rot-13 stops a locked body being read by accident or curiosity, which is all APL/360's binary workspaces stopped, and nothing more. An obscured file is not runnable as a program and says so in its preamble, printing one line and signing off rather than a screen of CHARACTER ERRORs, which is pinned as a reg-rs baseline. Anchored the work/ gitignore pattern to the repository root so the fixture library tests/scripts/work/ can be tracked; that also surfaced stale saved workspaces an old test run had left in the apl-session crate directory, now removed and confirmed gone. Split objects() out of write() when it went over the LOC gate. Also recorded step 008-command-error-messages from the owner's report that )LOAD RACE and )LOAD 1 RAXE both answer INCORRECT COMMAND when the manual has WS NOT FOUND; not fixed here. 9 new unit tests, 2 new reg-rs CLI tests (73 total), parity row, workspaces.md section, design.md D14, README paragraph. All gates green, sw-checklist 184 passed 0 failed.