Gave the workspace a size. New apl-space crate does the accounting from a model rather than from Rust's allocator: 16 bytes of descriptor, 4 an axis, 8 a number, 1 a character, 8 plus its characters for a name, header names plus body text for a function. The total is recomputed from the symbol table rather than carried along, so it cannot drift. Workspace.quota is session state (not saved), set by --ws-size, default 1048576. I-beam 22 now reports what is free instead of a constant. set and define return AplResult so the check sits at the one choke point every assignment goes through, which propagates to bind and charges a call's arguments. WS FULL is raised on assignment, definition, argument binding, and a LOAD or COPY too big to fit; LOAD and COPY are all-or-nothing, with the old workspace put aside and given back on any error, which needed Reply to carry whether a line failed and needed LOAD to feed its own CLEAR rather than clearing first. Found and fixed a real regression: sample 61's LIB lines pinned the contents of the user's own library 0, which broke when the owner saved a workspace; removed them, rebased, and recorded the underlying fix (a library-root option) against the library-workspaces step in plan.md. 23 new tests across apl-space, apl-workspace, apl-session and apl-ibeam; new sample 62-workspace-space, 68 reg-rs tests passing. Docs: parity WS FULL done, language 22 row, workspaces.md rewritten with the cost table, session.md, README, architecture.md, design.md D13. All gates green, sw-checklist 179 passed 0 failed 1 warning (Binary Freshness).