Filled library 1 and made it findable. Ships three workspaces, each earning its place: LIFE (Conway's Life on a torus), RACE (a horse race written to be read -- character matrix, mixed output, the conditional branch idiom, dynamic scoping) and EDIT (a workspace to practise the del editor on, whose FACT is wrong by one on purpose). Each carries a DESCRIBE and a HOWNAME, per design.md D8, and )LOAD still prints only the SAVED line. Added --library DIR, which sets the directory all libraries are under; that also closes the hazard recorded last step, since a sample or test that saves or lists no longer touches the user's own work/. Added scripts/check-provenance.sh to the gates: every workspace tracked under ws/ must carry a SOURCE directive naming sw-apl, and an untracked file under ws/ fails too, so IBM material cannot reach ws/ by accident -- it would take a deliberate false claim. )SAVE does not write the directive, because a mark a program stamps on everything asserts nothing. The shipped files were generated by running sw-apl and )SAVE rather than hand-written, and a test asserts they are still exactly what )SAVE writes, which makes the file format a regression test too. 4 new tests against the real files, new sample 64-library, 71 reg-rs tests, all gates green, sw-checklist 184 passed 0 failed.