Pinned the numeric boundaries and fixed one real bug on the way. The bug: the fuzz was applied to comparison, floor and ceiling but not to an argument that has to be a count, so (0.1+0.2)x10 printed as 3, floored to 3 and compared equal to 3, while iota of it was DOMAIN ERROR -- exactly the anomaly the manual's fuzz sentence exists to prevent. non_negative_int and int_vector now round through the same fuzz, which covers iota, reshape, take, drop and compress. Everything else was found and pinned rather than changed: a number is exact only up to 2*53 because everything is a double (my first test assumed i64 width and was wrong); )DIGITS caps integers too, so 10000000000 prints as 1E10, which follows the manual's sentence about significant digits although later APLs print integers in full; zero has no slack in a relative tolerance. parity.md gains a Numeric boundaries table whose middle column says, for each row, whether it is the manual's or ours -- that distinction was as much the deliverable as the tests. Split counts.rs out of iota.rs when the shared helper took the module over budget. 15 new tests, new sample 67-numeric-edges, 76 reg-rs tests, all gates green, sw-checklist 194 passed 0 failed. Also recorded step 003-ibeam-reference from the owner's question.