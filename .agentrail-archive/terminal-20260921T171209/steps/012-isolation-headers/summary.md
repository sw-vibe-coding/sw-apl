Both local servers now send the cross-origin isolation headers, so a local visit is isolated on the first response and never registers the service worker. sw.js stays as the GitHub Pages fallback. The refusal message is one line pointing at Help, which now carries the explanation. scripts/serve-pages.py replaces the python one-liner. check-pages keeps the header-free case and gains one that a host sending the headers loads once with no worker.