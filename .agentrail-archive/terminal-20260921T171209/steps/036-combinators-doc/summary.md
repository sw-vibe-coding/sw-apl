Wrote docs/combinators.md, the reference to every BIRDS function: call, arguments, result, example and errors, with APPLY's and DYAD's full glyph tables. Every example and error was run against the workspace first, which surfaced the monadic table lacking +, the RANK ERROR from a single glyph, DYAD's VALUE ERROR without FN, FACT on non-integers, and that the suspended function is the bird called. birds.md now points to it instead of duplicating it; linked from README and session.md.