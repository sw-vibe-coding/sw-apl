Published with a lightweight GitHub Actions workflow (.github/workflows/static.yml) that builds nothing and uploads the committed pages/ as the site, matching sw-fun/suduko rather than sw-mlpl's gh-pages mirroring. Corrected the earlier wrong instruction: Deploy from a branch offers only the repository root or /docs, so main + /pages never existed and main + / served the README through Jekyll. Switched the repository's Pages source to GitHub Actions and verified the published URL by driving it in a real browser: )LIB 1 lists the three workspaces and iota 5 runs.