pages/ is tracked in full with .nojekyll and build-info.json, built locally by just publish and pushed, as sw-mlpl does it. Found and closed two traps: wasm-pack writes its own .gitignore holding a star, which silently kept the whole bundle out of the commit, and pages/redistributed is now tracked so check-provenance guards the bundle's LICENSE and ATTRIBUTION too. check-pages now serves under a /sw-apl/ prefix and confirms nothing is fetched from the root. README carries the link. Also confirmed by test that Espanso works in the browser unchanged, by both its keystroke and paste injection methods. Pages is not enabled in repo settings; that is the owner's to do.