Promoted the aplterm/server prototype out of experimental into two component workspaces under the full gates: components/web (apl-wire protocol plus a Link trait, apl-serve session driver, sw-apl-server with a TCP listener and an HTTP/WebSocket listener serving a plain terminal page) and components/term (apl-keyboard, apl-paper, apl-typing, aplterm). Kept the prototype JSON frame as the protocol and added verbatim tolerance so nc is an emergency client. Added the browser transport, the terminal page, just demo with a scratch library 0, version blocks and agent help for both new binaries, and docs: terminal.md rewritten, architecture, parity, plan, README. Fixed a PTY test that only passed on Linux (accept inherits O_NONBLOCK on BSD). All gates green, reg-rs 80/80. Work is on branch feat/terminal-server, not main.