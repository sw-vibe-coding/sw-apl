Owner correction 2026-09-20: the mark is `⍝⊃⌊`, not `∩⊃⌊`.

Turned 33 degrees, cap-horseshoe reads as a lower-case n -- the
mark said "nPL". The lamp has a counter, so it turns into a proper
rounded A and the mark reads APL.

One string in `scripts/gen-icons.sh`. Regenerate the favicon and
the apple-touch icon, republish, and look at the published page.
