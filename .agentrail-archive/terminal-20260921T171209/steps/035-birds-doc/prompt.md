Owner, 2026-09-21: "where is the doc for BIRDS?"

There was none. BIRDS was documented inside the workspace -- DESCRIBE,
HOWBIRDS, NOTHERE -- in sample 72's comments, and in one row of
docs/session.md's library table. The other library 1 workspaces have
no page either, but they do not need one; BIRDS is the only one whose
ideas need more room than DESCRIBE has.

Write docs/birds.md: every bird with its lambda term, its APL\360
form and a run; the three kinds and why the line falls where it does;
how APPLY and DYAD reach the primitives, and the table as the limit;
dynamic scoping as the way the dyadic birds pass a function; and the
birds that are not there, each with what it would need. Link the
owner's post and the live demos as where the birds came from.

User-facing rules apply: what and how, no plans, no dates. Say what
APL\360 lacks as a fact about APL\360, and name no later APL. Link it
from the README and from session.md's library table.
