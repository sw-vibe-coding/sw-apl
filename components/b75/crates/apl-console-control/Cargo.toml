[package]
name = "apl-console-control"
description = "sw-apl console control: the IBM 5110's own system function, which only the (B) '75 mode has"
edition.workspace = true
license.workspace = true
version.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
apl-value = { path = "../../../value/crates/apl-value" }

[lints]
workspace = true
