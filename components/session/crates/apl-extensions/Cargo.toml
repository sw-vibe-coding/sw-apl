[package]
name = "apl-extensions"
description = "sw-apl's own system commands, which no historical system had: )DIALECT, )HELP, )LIBS"
edition.workspace = true
license.workspace = true
version.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
apl-eval = { path = "../../../eval/crates/apl-eval" }

[lints]
workspace = true
