[package]
name = "apl-reply"
description = "sw-apl session reply: the lines one input produced, and what the shell does next"
edition.workspace = true
license.workspace = true
version.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
apl-commands = { path = "../apl-commands" }

[lints]
workspace = true
