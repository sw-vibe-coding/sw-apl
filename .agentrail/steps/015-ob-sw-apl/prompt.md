Owner direction 2026-09-25, docs/plan.md (literate APL, with Org Babel). Write emacs/ob-sw-apl.el and emacs/sw-apl-mode.el: an Org Babel language sw-apl that runs a block through sw-apl --no-echo in batch, with a :mode 70 or 75 header argument (default 75), and a major mode whose font-lock faces cover primitives, system names and commands, numbers, strings, comments and the del, for htmlize to colour. ERT tests run with emacs --batch through a just recipe that skips cleanly where Emacs is absent. Document installing and using both (docs/literate.md or glyph-entry.md beside the Emacs keymap). Gates as usual; the README and markdown ASCII rules apply.