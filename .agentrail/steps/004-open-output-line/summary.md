An output line left open by quote-quad now carries across a statement in immediate execution, which is the prompt idiom and was the last todo row. The flag already existed as Shown.open -- which is why the function case worked -- and was dropped at the handoff to the caller; Reply now carries it with a split() mirroring Shown::split(), and both shells honour it: the batch runner writes the last line without a newline and skips the prompt, and the reader prompts with the open text so rustyline edits where the carriage is. respond also changed shape: the statement's result goes back into the workspace's pending output rather than being rendered separately, so one flush decides openness and a statement showing nothing cannot close a line an earlier one wrote to. The CLI test for this was named and described after the bug it pinned, so it was re-seeded rather than rebased. A good deal of budget work was forced along the way: a shared show() for both shells, without_shebang folded into lines, refused folded into run_command, and the input dispatch moved to commands.rs where the other routing lives. parity.md now has no todo rows, and its summary gives a check that matches table rows rather than its own sentence -- the first attempt counted itself. All gates green, 79 reg-rs tests, sw-checklist 199 passed 0 failed.