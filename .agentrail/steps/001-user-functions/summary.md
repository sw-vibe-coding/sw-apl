Defined functions in pure APL\360: del headers (NAME, NAME B, A NAME B, each with or without R<-, then ;LOCAL names), definition mode with the [n] prompt in the session and in batch echo, a function table in apl-workspace beside the variables, calls that bind arguments and shadow locals in a fresh frame and restore on exit, the result taken from the header's result variable at exit, VALUE ERROR for a result-less function used for its value, SYNTAX ERROR for the wrong valence, recursion bounded by DEPTH ERROR at 256 frames. Parsing is now name-class aware (F B is a call only when F takes a right argument; a niladic function is an operand). Two new crates keep the sw-checklist module gates at zero warnings: apl-scan (bracket matching, semicolon segments, what ends an operand, the del header) and apl-call (valence, frame, body, result, with 'evaluate one line' passed in as a function pointer). Added samples/53-functions.apl and seeded its reg-rs baseline; 39/39 reg tests pass. docs/parity.md, docs/architecture.md, README.md, samples/README.md updated. Commit f5a8d8a.