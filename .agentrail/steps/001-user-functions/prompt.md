Phase 3 step 1 (docs/plan.md). Defined functions in pure APL\360. Lexer already emits Del and DelTilde. Parse a del header into a Function definition: NAME | NAME B | A NAME B, optionally with R← in front, then optional ;LOCAL names. In the session, an opening del starts definition mode: subsequent lines are body lines (prompted [1], [2], ... per docs/session.md) until a closing del. Store functions in the workspace beside variables (a new apl-workspace type). Calling: a name that holds a function applies it with the valence written; bind arguments and locals to a fresh frame, shadow globals dynamically, restore on exit; a function with no result used for its value is VALUE ERROR; wrong valence is SYNTAX ERROR; recursion works; guard runaway recursion with DEPTH ERROR. Branching comes in the next step, so a body runs its lines in order and the result is R at exit. TDD: session transcript tests for definition and call; update docs/parity.md rows; add samples/53-functions.apl and seed it.