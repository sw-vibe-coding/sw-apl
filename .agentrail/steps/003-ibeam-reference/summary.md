Wrote docs/i-beam-reference.md: for each of the eight I-beams, what it reports, in what units, and a worked example of turning it into something a person would read -- a clock time from sixtieths of a second, a date taken apart with encode, kilobytes free. Covers what each reports when there is nothing to report, that the terminal count is always 1 because sw-apl runs on your machine, the errors, and how a clock-reading transcript is made reproducible. It got to write down two things that were nowhere: that the line-number I-beams read the state indicator rather than the call, so they report a suspended function from immediate execution too, and that the state indicator is innermost first. Ran every transcript before writing it down, which caught two of my own claims being wrong: processor time over sixty is 0 on a fast machine rather than the fraction I had, and a bracket after the argument is an index not an axis, so it is RANK ERROR while the axis form is SYNTAX ERROR. The error claims are deterministic, so sample 58 now makes them and the reference cannot rot without a baseline noticing; rebased for that reason. Also corrected parity.md's description of )MSG, )OPR and )PORTS as stubs -- they answer INCORRECT COMMAND. Linked from README, language.md and parity.md. No code change. All gates green, 76 reg-rs tests, sw-checklist 194 passed 0 failed.