Owner 2026-09-26: just pages-serve failed with a Python traceback because port 8361 was already taken by another server. Let the recipe take a port (default 8361), and have scripts/serve-pages.py say plainly that the port is in use, what to do (another port, or stop what holds it), and exit non-zero without a traceback. Same for just demo if its ports are taken, where cheap. Test the script's message; document in README and terminal.md.