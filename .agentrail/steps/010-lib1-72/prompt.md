Phase 9 step 10 (docs/plan.md, owner direction 2026-09-21). Library 1
for '72.

BIRDS first: in '72 the birds NOTHERE says need an execute can be
written, because a defined function becomes reachable by name. '72's
BIRDS flies them, and its NOTHERE says what is still missing and why.
Decide with the owner which of LIFE, RACE and EDIT '72 carries, and
whether any should use what '72 adds. Each is built by saving a
session, as '68's are, with the provenance mark and library tests.
Update docs/birds.md and docs/combinators.md for both modes.
