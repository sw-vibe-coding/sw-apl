Phase 9 step 8 (docs/plan.md). '72 gains the quad system variables,
in a new '72-only crate: the list and meanings from docs/aplsv.md.
Several stand for settings '68 reaches through commands and I-beams
(the index origin, print precision and width, the random link, the
clock); '72 must read and set the same state, not a second copy of
it. Samples for '72; '68 unchanged.
