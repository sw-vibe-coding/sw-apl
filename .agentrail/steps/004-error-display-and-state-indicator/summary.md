Suspension, the state indicator, and the FN[n] error display. An error inside a function now heads the echoed body line with NAME[n] in place of the six-space indent, with the caret under the point of detection in that line; AplError gained the context field architecture.md planned for, carrying function, line and statement. The function no longer unwinds: its activation stays on the stack with its locals readable, )SI lists innermost first with a star on the function the error came from, )SIV adds each call's local names in an aligned column, a bare right arrow clears the top entry together with the pendent callers that have nowhere to return to, and a right arrow with a line number takes the suspended function up again, returning through those callers so each carries on at its next line. Workspace.stack replaces the Frame value and the depth counter and serves as both the dynamic-scoping mechanism and the state indicator; apl-call gained stack.rs for entering, settling, clearing and resuming; Output::Branch carries an Option so a bare arrow is distinguishable from a blank line. Stated boundary, recorded as a parity row rather than hidden: sw-apl suspends a call written as a whole statement, while a call inside a larger expression unwinds, because half an evaluated expression cannot be taken up again. Quality work the owner asked for mid-step: apl-workspace and apl-call had no tests at all and own the subtlest machinery, so both gained test crates, apl-call driven by a stand-in evaluator through its Run function pointer; apl-editor gained one too, including a check that its glyphs match the generated table after a test of mine used the wrong del-tilde code point; the MAX_DEPTH constant now records that the guard must fire before the machine stack and which test notices when it stops doing so, after 256 overflowed. Three regressions in this step each earned a guard. docs/session.md's error example contradicted its own prose and was corrected to the APL\360 form. Sample 56 added and seeded, sample 53 rebased for the new display. 238 tests, 43 transcripts, all gates clean with only the expected Binary Freshness warning. Commit fcb3ceb.