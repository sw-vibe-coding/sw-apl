Phase 10 step 7 (docs/plan.md). docs/emulation-policy.md: what sw-apl does where the historical machine, not the language, decided. Classes: historical (as the machine did it), host (the modern host's own), fixed (a deterministic value), unavailable (not emulated), extension (sw-apl's own). Cases: the 5110's clock (quad TS is 1900 0 0 0 0 0 0, kept, owner 2026-09-22), the I-beam clock and processor time in (A), the multiuser commands of a time-shared 360 (unavailable), the terminals-connected I-beam, the workspace size, )SYMBOLS, the three sw-apl extension commands. parity.md keeps what is done and restricted and links here; README and )HELP RESTRICTIONS point to it. An --environment historical|host switch is the owner's decision: document it as not implemented, and ask.
