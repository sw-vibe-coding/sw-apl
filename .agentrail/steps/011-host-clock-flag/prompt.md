Phase 10, owner direction 2026-09-24 (low priority, last). A flag to use the host's clock instead of the historical one. The default stays historical: quad TS in (B) is 1900 0 0 0 0 0 0, and (A)'s I-beam clock is as it is. Plan the switch outside the language -- a --clock host|historical flag, or the emulation policy's --environment, and the same key in sw-apl.toml -- document how it is enabled in docs/emulation-policy.md and )HELP, and test both settings (quad TS, the I-beam date and time) with a fixed clock injected for the host case so the tests are deterministic. Never a quad name.
