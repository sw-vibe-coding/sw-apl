Owner direction 2026-09-20: the banner names a mode, not a version.

The header reads "sw-apl -- APL\360". The owner has a mode in mind
for it rather than a fixed name: the current one, and the only one,
is shown as

    Ⓐ '68

-- APL as it was about 1968 -- with a tooltip reading "APL\360
compatible". Make the header say that.

It is a label, so keep it one: a tooltip for the mouse, and the same
words where a screen reader and a touch screen can reach them,
because a touch screen has no hover and a reader who cannot see the
circled A has nothing to go on. The circled A should not be read out
as "circled Latin capital letter A".

Do not describe other modes anywhere. There is one mode, and the user
docs say what is, not what is planned.

**Owner clarification, 2026-09-20.** The header is the name and then
the badge:

    sw-apl  Ⓐ '68

and the tooltip, on the badge, is "APL\360 compatible". The name
stays; only "-- APL\360" goes, replaced by the badge.
