MVP step 1 of 2 (docs/plan.md Phase 1). Create component workspaces (each its own cargo workspace under components/, crates under crates/): value (apl-value: Array{shape,data}, Data::Num(Vec<Number>)|Char(Vec<char>), Number::Int(i64)|Float(f64) with promotion, AplError{kind,caret} with SYNTAX/VALUE/DOMAIN/RANK/LENGTH kinds), lex (apl-lex: numeric literals with high minus U+00AF and E notation, strands, names, glyphs + - × ÷ ⌈ ⌊ | * ← ⍝ ( ) and CHARACTER ERROR for anything else), parse (apl-parse: right-to-left AST, monadic/dyadic application, strands, assignment, parentheses, long right scope), eval (apl-eval: scalar extension with RANK/LENGTH errors, variables, VALUE ERROR), display (apl-display: integers, floats to 10 significant digits, high minus, single-space vectors). Replace the stub session in components/cli/crates/sw-apl/src/session.rs with a real one (or a new apl-session crate) so the REPL and batch modes evaluate lines; assignment displays nothing; errors print name, echoed statement, caret line per docs/session.md. TDD: unit tests per crate first, then run scripts/reg-seed.sh 01, 02, 03 and commit tests/reg-rs baselines. Read docs/language.md, docs/session.md, docs/glyphs.txt, docs/architecture.md first. Keep every crate inside the sw-checklist gates.