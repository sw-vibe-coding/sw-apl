Workspace libraries behind a store trait: apl-store with Files (filesystem, unmoved) and Memory (in-memory, library 1 host-filled and read-only, library 0 with a kept hook). Workspace.libraries becomes Workspace.store; apl-library path.rs becomes which.rs and no longer knows where anything is. In the browser )LIB 1 lists the three shipped workspaces, )LOAD 1 NAME works, and )SAVE writes library 0 into local storage via the page, since a worker cannot reach it. 14 store tests run against both implementations, existing session and library test assertions untouched, reg-rs 80/80, gates clean.