Repair the existing Linux portability failures found while validating the aplterm prototype: apl-cli-shebang-bare uses a multi-argument bare shebang that Linux passes as one argument; apl-cli-obscured-load and apl-cli-workspace-file have quoted heredoc delimiters that produce shell warnings. Preserve interpreter behavior and explain any intentional baseline changes. Verify all 80 existing regressions on Linux.