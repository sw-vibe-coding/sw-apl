A glyph used where it has no such form is now a SYNTAX ERROR, and ErrorKind::NotImplemented is removed from the vocabulary entirely, which takes the must-reach-zero parity row to zero. Six sites, all the same mistake: the scalar dispatch and its guards, scan, outer, inner and reduce's identity table, the axis helper, and the evaluator's monadic fallthrough. The axis one needed a decision rather than a sweep -- it distinguished a glyph that never takes an axis from one whose axis form is APL2, and that distinction is gone, because an APL2 form is not a form APL/360 has and a reply separating them would be telling the reader about an APL they are not using. The reverse mistake is the one to watch: a glyph that does have the form and was given an argument outside it is still a DOMAIN ERROR, and sample 69 puts those beside the syntax errors so the line between them is visible rather than asserted. Eleven existing assertions said NOT IMPLEMENTED and each was remade as a decision, which is why the axis assertion in session_tests carries a rewritten comment -- it was the only one whose old answer had an argument behind it. New sample 69-no-such-function, 78 reg-rs tests, parity's two rows closed, language.md now names the whole error vocabulary. All gates green, sw-checklist 199 passed 0 failed.