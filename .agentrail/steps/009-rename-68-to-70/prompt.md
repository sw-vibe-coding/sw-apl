Owner question 2026-09-21, answered yes: (A) '68 should be (A) '70, to match the APL\360 the owner used in 1972 -- the December 1970 APL\360-OS/DOS release, which is also where sw-apl's domino comes from (docs/citations.md). Same convention as (B) '75, named for the release used. Confirm with the owner in the step report that this is what they wanted. Change every non-historical reference from '68 to '70 for the (A) mode: docs (plan.md with a note, mode-b.md, prd.md, workspaces.md, session.md, parity.md, architecture.md), CLAUDE.md, README if it says '68, the header tab in pages/index.html (the circled-A tab and its aria-label), the --mode flag (accept 70 and A; decide whether 68 stays accepted as an alias for existing scripts, and say so), cli_help texts, the Mode::parse tests, the check-pages expectations, pending step prompts, and memory. Completed step records, saga.toml, step.toml descriptions, the saga plan.md and CHANGES.md commit subjects keep '68 as history; say so in docs/plan.md. Crate and directory names (components/a68, apl-a68-commands) are identifiers: decide whether to rename them (git mv, a commit of its own) and say why. reg-rs: the help transcript moves if the flag text changes -- rebase with that reason; nothing else may move. Gates, pages rebuild and check-pages, commit, push.