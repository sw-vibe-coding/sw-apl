Wrote docs/birds.md for the BIRDS workspace, which had no page: every bird with its lambda term, APL\360 form and a verified run; the three kinds and why the line falls there; glyph dispatch through APPLY and DYAD and the table as a real limit; dynamic scoping as how the dyadic birds pass a function; and the Mockingbird, Sage and Z with what each would need. Linked from the README and session.md. Every example checked against the running workspace.