Phase 4 step 2 (docs/plan.md). The workspace file: plain UTF-8 that a person can read and APL can re-execute. A saved workspace is del form for each function, assignment for each variable, and settings commands for the environment, so that a workspace file is also a sample and a sample is also a workspace. Write Saved out and read it back, and prove the round trip in a test: a workspace with variables of several ranks and types, functions with locals and labels, a non-default index origin, print precision and width, a random link that has been advanced, and a name. Decide what to do about the state indicator, which Saved holds: APL\360 saved suspended functions, but a re-executable text file cannot express a half-run function, so either leave it out and say so in docs/session.md and docs/parity.md, or find a form that works. Say which and why in the commit message. Order the output so the file is stable: the same workspace must write the same bytes twice, which a HashMap will not give you. Pin the file itself as a fixture so its shape is a baseline, not only its round trip. The file is NAME.apl.ws per docs/design.md D7; check that D7 still says what this step does, and fix it if not. TDD; reg-rs for anything run through the binary; update docs/parity.md in the same commit; commit, push, report.