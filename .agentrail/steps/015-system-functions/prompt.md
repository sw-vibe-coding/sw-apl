Phase 9 step 9 (docs/plan.md). '75 gains the quad system functions,
in a new '75-only crate: the list from docs/aplsv.md. Defining a
function from characters and getting its characters back meet the del
editor and locked functions: a locked function's characters are not
to be had by any route, and a function defined this way must be one
the editor could have made. Samples for '75; '70 unchanged.

**Carried from step 014 (system variables):** ⎕DL. The 5110 manual
lists it with the compatibility variables (value 0), but in APLSV it
is a delay function; decide which it is here. The ⎕ names lex as one
name in (B) already (apl-sysvars reads and assigns the variables);
a system function needs the parser to take a quad name as a function,
where today one beside an argument is a SYNTAX ERROR. A function
defined by ⎕FX may localize system variables in its header only once
step 016 does.
