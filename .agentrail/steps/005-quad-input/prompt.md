Phase 3 step 5. ⎕ on the right of an expression prompts (the APL\\360 way) and evaluates a typed line in the current context, in immediate execution and inside functions; ⍞ reads characters raw and, on the left of an assignment, writes without a trailing newline. Batch mode takes the next input line from the script. Interrupt during input. TDD with session transcripts that feed input; parity rows; sample.