A board on the page, drawn from the same keymap.json apl-keyboard compiles in, laid out like the 2741, each key carrying both its faces with an ABC layer so a touch screen reaches letters too. Taps go through the same Board a keystroke does. Named for a screen reader out of data/glyphs.toml. The redistributed picture stays reference, shown under Help with its author and licence. Footer prose moved behind Help and the header cut to half a line to make room. Fixed two bugs the owner found: a shadowed channel binding that left Keyboard dead, and a layer toggle that typed the word layer.