manifest.json with relative start_url and scope so it installs from a project sub-path, the apple-mobile-web-app meta tags, a theme-color per scheme, and icons: the favicon is del made with the favicon CLI, and the install icons are the README logo scaled, transparent for any and inside the maskable safe zone. scripts/gen-icons.sh makes them and they are tracked with the rest of pages/. Six new check-pages cases including that every icon named really exists.