Owner bug report 2026-09-24, from the sw-apl-workspaces agent; blocks LEARN, COURSE and DRILL. (1) A system command typed in reply to quad input is reported as SYNTAX ERROR against the reading line. APL 360 ran the command and repeated the input request: inquiries such as )LIBS, )LIB, )WSID, )DIALECT, )HELP, )FNS, )VARS and )SI run and the quad prompt returns; )LOAD, )CLEAR and )OFF may abandon the request. Check the 5110 manual for (B) as well. (2) An error in the reply to quad input prints its report against the reading function's line, while )SI names the caller as suspended and a branch to 1 restarts the caller: one of the two is wrong. Check whether the manual repeats the input request after an error rather than suspending, and make the display and )SI agree. Repro: define R gets quad in F, and T that prints X then calls F; run T; at the quad prompt type )SI, then 1 2 3+4 5. TDD: session tests for each case in both modes, a sample for the transcript, parity.md and session.md rows. Quote-quad input takes the line as characters, so only quad input is affected; confirm with a test.