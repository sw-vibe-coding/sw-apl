Owner report 2026-09-24: in (B) the browser shows missing-glyph boxes for quad AV. The atomic vector is correct as built: a position with no character sw-apl can hold (reserved or unused, control codes, the underscored letters that are two code points here) holds U+E000 plus its index, in Unicode's Private Use Area, so every position is distinct and quad AV iota quad AV works (docs/mode-b.md). No font draws those, so they show as boxes.

1. Display, not data: the page, and the CLI and aplterm consoles, draw a Private Use Area placeholder as a dim middle dot, with its code point on hover in the browser (a span with a title). The characters themselves are unchanged -- quad AV, iota, equality and saved workspaces see U+E000 plus index -- so no reg-rs transcript moves unless one prints a placeholder, and then only with a stated reason. A test pins that quad AV still holds 256 distinct characters.
2. A web font: the page asks for APL385 Unicode but only has it if the reader installed it, so APL glyphs can box on a machine without it. Bundle an APL font as a web font (@font-face, woff2, font-display swap) only after checking its licence: APL385 Unicode's terms, or JuliaMono (SIL OFL) as the alternative. It is third-party material: images/redistributed/ with LICENSE and ATTRIBUTION.md, copied into pages/ with them, check-provenance covering it, docs/citations.md listing it. If no font's licence allows redistribution, say so and ship the placeholder display alone.

TDD: check-pages -- quad AV displays without a PUA character on the paper (placeholders are spans), the font loads (document.fonts check) and nothing 404s. Docs: mode-b.md (the atomic vector's display), terminal.md, README licence section.
