The underscored alphabet A-underbar to Z-underbar: twenty-six further characters of the APL\360 set, letters in their own right and distinct from the plain letter. Chose the letter plus U+0332 COMBINING LOW LINE over inventing a code point per letter (Dyalog maps them to circled capitals for its atomic vector, APL2 to lowercase; neither suits an APL\360 with lowercase already a distinct name letter), and recorded the cost: nothing in names, one predicate in display, one element too many in character data. strike() grew a rule rather than twenty-six rows and now returns text, since the glyph is two code points; a test checks no table pair collides with the rule. Added columns() and pad() in apl-glyphs and routed WIDTH wrapping, FNS/VARS/SI columns and the error caret through them. Merged apl-lex's two name predicates into is_name_char(c, prev) to stay inside the function budget. The low line alone now names itself in CHARACTER ERROR. Sample 71 extended, reg-rs rebased. parity.md overstrike row back to done. All gates clean, pushed as e2e7355.