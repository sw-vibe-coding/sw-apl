Phase 10 step 5 (docs/plan.md). Libraries by URL in the browser. The page reads pages/libraries.json (number, name, URL of an index file); each library is a static site -- the owner's other repository published on GitHub Pages, which answers with Access-Control-Allow-Origin: * -- whose index lists its workspace files. The page fetches them before the session starts, with the rest of its startup, and hands them to the worker as library 1 is handed to it today; )LIB N, )LOAD N NAME and )LIBS then work as at the CLI. A library that does not load says so on the paper and the session starts without it. Cross-origin isolation (COEP require-corp) must still hold: check the fetch works under the service worker.

TDD: check-pages with a second local server standing in for the other repository, serving an index and a workspace: it loads in the right mode and not the other; a missing library is reported. Docs: README, terminal.md, the Help dialog.
