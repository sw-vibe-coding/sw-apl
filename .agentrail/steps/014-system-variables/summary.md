(B) 75 gains the quad system variables in a new b75 crate, apl-sysvars, built from the IBM 5110 APL Reference Manual Chapter 5 and Appendix B. In (B) a quad before a letter lexes as one name (data/glyphs.toml system_names); (A) is unchanged, SYSTEM ERROR-free: quad IO is still a SYNTAX ERROR there. IO, PP, PW, RL read and set the same saved.env and saved.print fields the 70 commands and directives use, through apl-settings bounds (the random link joined them). CT reads 1E-13, other values NONCE. LC, WA, AV, TT, UL report and ignore assignment; AI and TS hold the 5110 fixed values (no clock) and take new numbers; LX is saved, makes a workspace (B)-only, and runs after LOAD in (B); COPY leaves it unless named. AV follows Appendix B with private-use characters where sw-apl has no single character. The (B) clear workspace is PP 5, PW 64, and shows whole numbers up to ten digits in full (apl-display Precision). Sample 77; reg-rs 86 of 86, no (A) transcript moved. Inserted step 016 for settable CT, localized system variables, PW 128 while defining, indexed assignment; DL carried to step 015. Commit 673fc48 pushed.