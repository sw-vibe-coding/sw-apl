A colophon below the fold with copyright, MIT licence, repository link and the build facts (host, short SHA, timestamp, bundle digest) read from build-info.json, which publish-pages.sh now stamps with the host. The session became a wrapper owning the viewport exactly so none of the transcript's room was given back; the Keyboard/Help bar stopped calling itself a footer. Moving the session into a wrapper moved the board's placement ground, which is fixed and checked.