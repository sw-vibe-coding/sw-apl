Font half of the atomic-vector report (step 004). Vendor APL385 Unicode (Adrian Smith, public domain; obtained via abrudz/APL386) as woff2 in images/redistributed/apl385-font with LICENSE and ATTRIBUTION.md; just pages copies it into pages; the page's CSS loads it with font-display swap and falls back as today. check-pages: the font loads and nothing 404s. docs/citations.md and the README licence section name it. Provenance gate passes.