Owner request: in the interactive session, up arrow recalls prior input for editing or re-submission (e.g. +/?6 6 repeated with up-arrow, enter). Use rustyline in the sw-apl CLI: six-space prompt, Unicode input, history kept in a per-user file, Ctrl-C cancels the line, Ctrl-D ends like )OFF. Batch mode unchanged. Keep the cli crate inside the gates (merge decode into shell, add a repl module). Re-render the vhs tape to confirm it still records. Update docs/session.md and parity.