Phase 10 step 3 (docs/plan.md). )HELP, an sw-apl extension, for discovery. With no argument: the commands the current mode has, grouped as the manuals group them (workspace, library, inquiry, terminal), each marked historical or an sw-apl extension, and a line on how to ask more. With a topic: a short page -- a command by name ()HELP LOAD, )HELP LIBS, )HELP DIALECT), and topics EXTENSIONS, RESTRICTIONS and MODES. An unknown topic says so and lists the topics.

The text is data (a file under data/, read at build time), shared by the CLI, the service and the browser, and says only what the current mode has -- (B) has no )ORIGIN to explain. Lines fit 64 columns, the (B) screen. What and how, no plans or dates. The browser's Help button stays; say in it that )HELP exists.

TDD: every command the session has is covered by a help page, and every page names only commands the mode has; a sample for each mode.
