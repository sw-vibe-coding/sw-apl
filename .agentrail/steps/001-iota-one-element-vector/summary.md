The index generator takes a scalar or a one-element vector, so iota of the shape of a vector works and the nub idiom with it. A unit test that had asserted the bug was rewritten. Checked the other primitives for the same assumption: deal and rotate have it, recorded as step 002 rather than folded in. Sample 73 builds the set operations APL\360 has no primitives for. reg-rs 82 of 82.