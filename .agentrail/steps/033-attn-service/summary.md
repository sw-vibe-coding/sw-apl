ATTN works from every terminal. Each connection is read on its own thread the whole time, raising the session's flag the moment {"attn":true} arrives and queueing other lines; the link shuts the connection down when dropped so nc still sees the hang-up. The WebSocket stays one object behind a lock with a 20 ms read timeout rather than being split. aplterm reads frames on a thread so stdin never changes hands, and while the service works holds raw mode for the whole wait with the keyboard locked as a 2741's was, only Escape, Ctrl-[ and Ctrl-C live. The served page sends ATTN on Escape. Pushed as 95b1ee8.