Owner, 2026-09-21: "also need a ./docs/combinators.md document for the
bird workspace functions/usage".

docs/birds.md explains BIRDS: why APL\360 can write only some birds,
the three kinds, glyph dispatch, dynamic scope, what is missing. This
is the other thing a reader wants: a reference to every function in
the workspace -- how to call it, what it takes, what it gives back,
the errors it can raise, and an example -- including APPLY's and
DYAD's tables of glyphs and what each glyph does.

Every syntax, result and error in it is run against the workspace
before it is written down. Cross-link it with birds.md, trimming from
birds.md whatever becomes the reference's job so the two do not say
the same thing twice. Link it from the README and session.md.
