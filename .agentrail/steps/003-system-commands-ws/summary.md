Implemented the workspace system commands: )SAVE )LOAD )DROP )LIB )COPY )PCOPY )CONTINUE. Split a new apl-commands crate out of apl-session (command/save/load). Added Answer.feed so a command can hand lines back for the session to run as typed -- that is how a re-executable workspace file's ) commands and del definitions take effect on )LOAD and )COPY. )COPY takes only definitions so donor settings stay in the donor. Added Workspace.libraries so the library root is per-session state rather than the process CWD, which also removed a parallel-test race; library 0 is <root>/work and the first )SAVE creates it. )LOAD prints only the SAVED line, per the APL/360 transcript. Reconciled the clear-workspace reply to NOT SAVED: THIS WS IS CLEAR WS. 8 integration tests, sample 61-save-load seeded into reg-rs (67 passing). docs/workspaces.md gained sections on naming a workspace and on available space (I-beam 22); parity, plan, README, samples/README updated. All gates green.