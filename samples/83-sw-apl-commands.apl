⍝ The commands sw-apl adds, which no historical system had. They are
⍝ system commands, never quad names, so no program can depend on
⍝ them.
⍝ Which mode this session is in. It only asks: the mode is chosen
⍝ when the session starts.
)DIALECT
)DIAL
)DIALECT 75
)OFF
